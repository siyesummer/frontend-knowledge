# 第七套准生产发布演练验收清单

## 阶段 1：music-api:0.0.1

- [x] 服务器 `3100` 启动前未被占用
- [x] 原 `music-api.service` 保持 `active`
- [x] Compose 只包含 `image:`，不存在 `build:`
- [x] 已执行 `docker compose pull music-api`
- [x] 已记录 `siyesummer/music-api:0.0.1` 的 RepoDigest
- [x] `drill7-music-api` 为 `running healthy`
- [x] `RestartCount` 为 `0`
- [x] 服务器 `127.0.0.1:3100/` 返回成功
- [x] 服务器 `127.0.0.1:3100/search` 返回业务结果
- [x] 本地电脑可访问 `203.0.113.10:3100/search`
- [x] 本地 siyeWorld 搜索和用户歌单请求正常
- [x] 允许来源的 CORS 预检返回 `204` 和精确 Origin
- [x] 未授权来源没有 `Access-Control-Allow-Origin`
- [x] Docker 日志无持续异常
- [x] 原 systemd `127.0.0.1:3000/search` 仍正常
- [ ] 腾讯云 `3100` 安全组规则仅临时开放并限制来源 IP

## 验证证据

```text
验证时间：2026-07-16 15:10:43 CST
Git Tag：music-api-v0.0.1
镜像标签：siyesummer/music-api:0.0.1
RepoDigest：sha256:effd6e0ac198e62e9b27804bc5a5c0066b7eb71b4efc043e57130c970b14732d
容器状态：running / healthy
RestartCount：0
本地前端 Origin：http://localhost:8080
本地前端验证结果：搜索、用户歌单、歌曲详情等真实请求均返回 200；公网直连绕过本地代理后仍返回 200
原 systemd 链路验证结果：music-api.service 为 active，127.0.0.1:3000/search 返回 200
异常与处理：未发现持续异常；演练期间用户确认手动开放 3100 且暂时接受全部 IPv4 可访问，作为已知安全例外；统一入口验证后必须删除该入站规则
```

## 阶段 2：linux-server:0.0.1 + MySQL

- [x] GitHub Actions `Release linux-server image #1` 全部成功
- [x] Git Tag / GitHub Release 为 `linux-server-v0.0.1`
- [x] Docker Hub 已生成 `0.0.1` 和 `sha-a49c3a45ff6c2a1ef771cd241013b592c4f0cffe`
- [x] Docker Hub 同时包含 `linux/amd64` 和 `linux/arm64`
- [x] Docker Hub Description 已同步 `deploy/README.md`
- [x] 服务器 `8181` 启动前未被占用
- [x] 已备份服务器阶段 1 Compose 和 `.env`
- [x] 服务器 `.env` 保持 `600`，真实 MySQL 密码未进入上传包
- [x] Compose 不存在 `build:`，MySQL 不存在 `ports:`
- [x] 已记录 `siyesummer/linux-server:0.0.1` 的 RepoDigest
- [x] `drill7-mysql` 为 `running healthy`，`RestartCount=0`
- [x] `drill7-linux-server` 为 `running healthy`，`RestartCount=0`
- [x] `drill7-linux-server` 以非 root UID/GID `10001:10001` 运行
- [x] MySQL 只加入 `data-net`，没有宿主机 `3306` 映射
- [x] `linux-server` 可通过服务名 `mysql:3306` 连接数据库
- [x] Spring Boot 已幂等创建 `chat_message`
- [x] 服务器 `127.0.0.1:8181/health` 返回 `{"status":"UP"}`
- [x] 服务器日志查询、聊天写入和历史查询均返回成功
- [x] 允许来源的 CORS 返回精确 Origin，未授权来源被拒绝
- [x] 本地 siyeWorld 通过 `203.0.113.10:8181` 完成聊天保存、历史查询和日志查询
- [x] 原 systemd `linux-server.service` 保持 `active`，`127.0.0.1:8081` 链路未受影响
- [x] Docker 日志无持续异常

### 阶段 2 验证证据

```text
验证时间：2026-07-16 17:30:56 CST（服务器冒烟），17:37:41 CST（本地前端）
GitHub Actions Run：29485330681
Git Tag：linux-server-v0.0.1
源码提交：a49c3a45ff6c2a1ef771cd241013b592c4f0cffe
镜像标签：siyesummer/linux-server:0.0.1
RepoDigest：sha256:46e7a9f7af1dd13464af5493690f7e0269f1dd7be34d9e311792cdf1c2b1f9fd
MySQL 容器状态：running / healthy / RestartCount=0，无宿为端口映射
linux-server 容器状态：running / healthy / RestartCount=0，UID/GID=10001:10001
本地前端验证结果：日志查询和聊天历史 GET 均返回 200；三条消息通过 POST 保存并可在日志页读取，MySQL public_room_total=3，没有重复 message_id；client_ip=192.0.2.10
原 systemd 链路验证结果：linux-server.service 为 active，127.0.0.1:8081 日志查询返回 200
异常与处理：无阻断异常；MySQL 初始化、时区文件和自签名 CA 警告属于官方镜像非阻断日志
```

## 后续阶段

- [x] 阶段 2：部署并验证 `linux-server:0.0.1` + MySQL
- [x] 阶段 3：部署并验证 `easy-chat:0.0.1`
- [x] 阶段 4：加入 edge-nginx 统一入口
- [x] 阶段 5：发布和部署 `siye-world:0.0.1`
- [ ] 阶段 6：执行版本升级和回滚

## 阶段 3：easy-chat:0.0.1

- [x] GitHub Actions 已推送 `siyesummer/easy-chat:0.0.1` 和 commit SHA 标签
- [x] Docker Hub Description 已同步 `deploy/easy-chat/README.md`
- [ ] Git Tag / GitHub Release 为 `easy-chat-image-v0.0.1`
- [x] Docker Hub 同时包含 `linux/amd64` 和 `linux/arm64`
- [x] 服务器 `3130` 启动前未被占用
- [x] 已备份服务器阶段 2 Compose 和 `.env`
- [x] 服务器 `.env` 保持 `600` 且未被上传包覆盖
- [x] Compose 不存在 `build:`，只新增 `easy-chat` 服务
- [x] 已记录 `siyesummer/easy-chat:0.0.1` 的 RepoDigest
- [x] `drill7-easy-chat` 为 `running healthy`，`RestartCount=0`
- [x] 容器以非 root `node` 用户运行
- [x] `easy-chat` 只加入 `edge-net`，不加入 `data-net`
- [x] 服务器 `127.0.0.1:3130/health` 返回成功
- [x] 允许来源的 Socket.IO polling 握手返回许可头
- [x] 未授权来源的 Socket.IO 握手被拒绝
- [x] `clientMsg -> backClientMsg` 消息回传成功
- [x] 本地 siyeWorld 通过 `203.0.113.10:3130` 完成真实聊天广播
- [x] 消息仍由前端调用 `203.0.113.10:8181` 持久化且无重复记录
- [x] 原 systemd `easy-chat.service` 保持 `active`，`3030` 链路未受影响
- [x] 阶段 1、2 三个容器仍为 `running healthy` 且数据未丢失
- [x] Docker 日志无持续异常
- [ ] 腾讯云 `3130` 对全部 IPv4 的临时规则需在统一入口验收后删除

### 阶段 3 验证证据

```text
验证时间：2026-07-16 22:13 CST
Git Tag：easy-chat-image-v0.0.1
源码提交：3d07580aab723b959256162bfb883daa03ce8ba7
镜像标签：siyesummer/easy-chat:0.0.1
RepoDigest：sha256:5c7d7374a871094b7bd1a870603bbccc30737f8f2553bf9172bd3b73a1384be7
容器状态：drill7-easy-chat running / healthy / RestartCount=0，user=node，3130 -> 3030，只加入 edge-net；四个第七套容器全部 healthy/0
本地前端验证结果：localhost:8080 通过 203.0.113.10:3130 完成 Socket.IO polling 和实时广播；POST 203.0.113.10:8181/api/chat/messages 返回 200，刷新后历史接口读取到新消息
MySQL 验证结果：message_id=uT256RN29ZXGGoVEXTvQPKHpruEJS983，content=新的，client_ip=192.0.2.10，无重复 message_id
原 systemd 链路验证结果：easy-chat.service 为 active，127.0.0.1:3030/health 返回成功
异常与处理：无阻断异常；3130 当前按用户选择临时对全部 IPv4 开放，统一入口验收后必须删除安全组规则
```

## 阶段 4：edge-nginx 统一入口

- [x] 服务器 `8090` 启动前未被占用
- [x] 已备份服务器阶段 3 Compose、`.env` 和演练文档
- [x] 上传包不包含 `.env`，服务器 `.env` 保持 `600`
- [x] Compose 不存在 `build:`，MySQL 仍无宿主机端口
- [x] edge-nginx 使用官方固定版本镜像并记录 RepoDigest
- [x] `nginx -t` 语法检查通过
- [x] `drill7-edge-nginx` 为 `running healthy`，`RestartCount=0`
- [x] edge-nginx 只加入 `edge-net`，不加入 `data-net`
- [x] `127.0.0.1:8090/health` 返回成功
- [x] `/health/music-api`、`/health/easy-chat`、`/health/linux-server` 均成功
- [x] `/music-api/search` 返回业务结果且正确移除路由前缀
- [x] `/socket.io/` polling 和 WebSocket 升级通过
- [x] `/api/logs/query`、聊天保存和历史查询均成功
- [x] 允许来源的 CORS 返回精确 Origin，未授权来源被拒绝
- [x] 本地 siyeWorld 四项服务地址全部切换到 `203.0.113.10:8090`
- [x] 浏览器 Network 面板不再请求 `3100/3130/8181`
- [x] MySQL 新消息唯一存在且客户端 IP 识别未回退
- [x] 原四个容器和三个 systemd 服务未受影响
- [x] Docker 日志无持续 `502/504`、upstream 或连接异常
- [x] 腾讯云 `8090` 仅作为演练入口临时开放

### 阶段 4 验证证据

```text
验证时间：2026-07-17 22:04:47 CST（真实消息），22:07:02 CST（日志聚合）
镜像：docker.m.daocloud.io/library/nginx:1.27-alpine，IMAGE_ID=sha256:65645c7bb6a0661892a8b03b89d0743208a18dd2f3f17a54ef4b76fb8e2f2a10，linux/amd64
RepoDigest：docker.m.daocloud.io/library/nginx@sha256:65645c7bb6a0661892a8b03b89d0743208a18dd2f3f17a54ef4b76fb8e2f2a10
容器状态：music-api、linux-server、MySQL、easy-chat、edge-nginx 全部 running / healthy / RestartCount=0；edge-nginx 使用 8090 -> 80 且只加入 edge-net
统一入口验证结果：服务器和 Windows 公网均通过 8090；/music-api/search、/api/logs/query、/socket.io polling 返回 200，允许 Origin 成功，未授权 Origin 返回 403
本地前端验证结果：四项 VUE_APP 地址全部切到 203.0.113.10:8090；音乐搜索、Socket、聊天历史、消息保存和日志查询成功，Network 未再请求 3100/3130/8181
MySQL 验证结果：message_id=uS4mV1mPaPImjcwdpFduFvVD1yq4hOVE，content=厉害了，client_ip=192.0.2.11，save_count=1
原链路验证结果：music-api.service、linux-server.service、easy-chat.service 均为 active；阶段 1-3 临时直连端口仍保留用于回滚
异常与处理：启动 edge-nginx 时 Compose 重新创建了 music-api 和 linux-server，二者快速恢复 healthy，MySQL 未重建且数据未丢失；唯一 Nginx error 为浏览器自动请求 /favicon.ico 产生的无害文件不存在日志，无 502/504 或 upstream 异常
```

## 阶段 5：siye-world:0.0.1

- [x] 本地 `yarn verify` 和 `.env.production` 生产构建通过
- [x] Docker Desktop 已成功构建 `siye-world:local`
- [x] 本地容器为 `healthy`、`RestartCount=0`，`nginx -t` 通过
- [x] 本地容器首页和 `/log-query` 返回同一 SPA index
- [x] 本地演练镜像制品只包含预期的 `203.0.113.10:8090`，不包含 localhost 或未启用的正式业务域名
- [x] GitHub Actions `Release siye-world image` 全部成功
- [x] Git Tag / GitHub Release 为 `siye-world-image-v0.0.1`
- [x] Docker Hub 存在版本标签和 commit SHA 标签
- [x] Docker Hub 同时包含 `linux/amd64` 和 `linux/arm64`
- [x] Docker Hub Description 已同步 `deploy/siye-world/README.md`
- [x] 已记录 `siyesummer/siye-world:0.0.1` RepoDigest
- [x] Compose 不存在 `build:`，siye-world 没有宿主机端口
- [x] siye-world 只加入 `edge-net`
- [x] 三个后端 CORS 白名单均包含 `http://203.0.113.10:8090`
- [x] `drill7-siye-world` 为 `running healthy`，`RestartCount=0`
- [x] edge-nginx 容器内可访问 `siye-world:80/health`
- [x] `/` 返回前端页面，`/health` 仍返回网关健康 JSON
- [x] `/log-query` 直接访问和刷新均返回前端页面
- [x] 前端制品只包含预期的 `203.0.113.10:8090` 演练地址，不包含 localhost 或未启用的正式业务域名
- [x] 音乐、Socket.IO、聊天保存、历史查询和日志查询均通过统一入口
- [x] 浏览器 Network 不再请求 `3100/3130/8181`
- [x] 新聊天消息在 MySQL 中 `save_count=1`
- [x] 六个容器全部 `healthy/0`，三个 systemd 服务仍为 `active`
- [x] edge-nginx 重建后日志无持续 `502/504` 或 upstream 异常
- [x] 阶段 5 完整验收前保留 `3100/3130/8181` 回滚入口
- [x] 阶段 5 不上传或启用 `siyefun.top` 配置，不修改 Windows 旧配置

### GitHub Pages 演练制品准备

- [x] 新增 `.env.pages`，复用当前 `203.0.113.10:8090` 演练地址
- [x] Pages 资源前缀为 `/siyeWorld/`
- [x] Pages 使用 `hash` 路由，避免刷新依赖 Nginx history 回退
- [x] `yarn build:pages` 构建通过并包含 `.nojekyll`
- [x] 已确认当前不创建或启用 Pages 发布 Workflow
- [x] 已记录限制：HTTPS Pages 会拦截 HTTP `8090` Mixed Content，当前只能验收静态页面、资源和路由
- [ ] 三个正式 HTTPS 服务域名可用后重新构建，才进行 Pages 完整业务验收

### 阶段 5 验证证据

```text
验证时间：2026-07-18 11:44 CST
GitHub Actions Run：发布成功，Run 编号未记录
Git Tag：siye-world-image-v0.0.1
源码提交：2ac790c806658a583ea00e1f8a11ec61e819c305
镜像标签：siyesummer/siye-world:0.0.1
RepoDigest：sha256:e3260fccde648467f94c581b83301f1548784b663572a551d536e6330c058694
容器状态：music-api、linux-server、MySQL、easy-chat、siye-world、edge-nginx 全部 running / healthy / RestartCount=0
SPA 路由验证：/ 与 /log-query 均返回同一前端 index.html，浏览器直接刷新 /log-query 成功
完整业务验证：浏览器音乐搜索、Socket.IO、聊天保存、历史读取和日志查询均通过 203.0.113.10:8090 返回 200；Network 未请求 3100/3130/8181
MySQL 验证结果：message_id=ugN4Bc047ZgoMLvTjV9Yk0LuCodbVmWb，content=看上去通了，client_ip=192.0.2.11，save_count=1
原 systemd 链路验证结果：music-api.service、linux-server.service、easy-chat.service 均为 active；服务及全部配置未改动
异常与处理：重建三个后端容器后 edge-nginx 仍缓存旧容器 IP，三条代理短暂返回 502；后端直连均健康，启动 siye-world 后仅重建 edge-nginx 即恢复，重建后无持续 502/504
```

## 阶段 6：siye-world 真实升级与回滚

- [ ] `siye-world:0.0.2` 已通过 GitHub Actions 正式发布
- [ ] `0.0.2` 与 `0.0.1` 的 RepoDigest 不同，且具有可验证的版本变化
- [ ] `0.0.2` 的 `/release.json` 与 OCI 标签均包含正确版本和 commit
- [ ] 升级前已备份服务器 `.env` 并记录 `0.0.1` 镜像 ID、digest 和 MySQL 数据基线
- [ ] 升级只重建 `siye-world`，待其健康后只重建 `edge-nginx`
- [ ] 升级后六个容器全部 `healthy/0`
- [ ] 升级后 SPA、音乐、Socket.IO、聊天、日志和 MySQL 唯一保存均正常
- [ ] 已把 `SIYE_WORLD_IMAGE` 恢复为 `siyesummer/siye-world:0.0.1`
- [ ] 回滚后再次只重建 `siye-world` 和 `edge-nginx`
- [ ] 回滚后容器实际镜像 ID 与阶段 5 的 `0.0.1` 基线一致
- [ ] 回滚后升级期间产生的 MySQL 消息仍存在且 `save_count=1`
- [ ] 阶段 6 全程仅只读检查 systemd；三个服务和全部配置未改动

## 后续 Windows 发布演练（不属于阶段 5）

- [ ] 从已通过 Linux 验收的同一正式 Git commit 生成 Windows 版本化 dist
- [ ] 确认阿里云 Windows 服务器不使用 Docker
- [ ] 备份旧 dist、旧 Nginx 配置和当前进程状态
- [ ] 新增独立 Nginx 配置文件，不编辑或覆盖旧配置
- [ ] 使用独立端口或测试 Host 灰度验证新 dist 和代理链路
- [ ] 验证 history 刷新、音乐、Socket.IO、聊天历史和日志查询
- [ ] 按真实 `http://siyefun.top` Origin 更新并验证 Linux 后端 CORS
- [ ] 准备新旧配置切换步骤和一键回滚步骤
- [ ] 正式切换前再次确认 Windows 当前不启用 HTTPS
